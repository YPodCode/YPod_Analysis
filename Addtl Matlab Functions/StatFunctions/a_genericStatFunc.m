function statObj = a_genericStatFunc(X, Y, y_hat, valList, fittedMdls, settingsSet)
%genericStatFunc BRIEFLY DESCRIBE FUNCTION HERE
%These are meant to be functions that numerically describe performance 
%characteristics of the models.  Examples would be the RMSE, R^2, etc.

%statObj can be anything (a table, array, single value, etc)
statObj = zeros(10,10);

end

